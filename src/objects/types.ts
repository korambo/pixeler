export enum Orientation {
  right = 'right',
  left = 'left',
}

export enum Boundary {
  top = 'top',
  right = 'right',
  bottom = 'bottom',
  left = 'left',
  full = 'full',
  vertical = 'vertical',
  horizontal = 'horizontal',
}
